import styles from './unknown.module.scss'

function Unknown() {

	return (
		<div className={styles.main}>
			<h1>You've found a page that doesn't exist.</h1>
			<p>Please navigate back to one that does using the menu.</p>
		</div>
	)

}

export default Unknown